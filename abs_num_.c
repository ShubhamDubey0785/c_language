#include<stdio.h>
#include<math.h>
#include<stdlib.h>
int main()
{
    int user;
    printf("User enter their number here: ");
    scanf("%d",&user);
    printf("The absolute value of the number enter by the user is %d",abs(user));
    return 0;
}